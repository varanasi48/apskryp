version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm start
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: src
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
